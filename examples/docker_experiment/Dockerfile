FROM python:3.12-slim
WORKDIR /workspace

# Install any dependencies your experiment needs here.
# Environment files (from experiment.toml [environment_files]) should be
# copied to their configured destinations.
#
# Example:
# COPY data/train.csv /data/train.csv
