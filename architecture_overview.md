# MPRun architecture overview

This is a general overview on how this system is *supposed* to work.

## Experiments and Runs

An *Experiment* is a program that needs to be run within a specified *parameter-space*.
As the number of possible permutations generated by the parameter-set can get large quickly, it becomes necessary, to run multiple instances in parallel to cut down on total runtime.

Each instance of an *Experiment* is called a *Run*.
It inherits the program and metadata from its parent *Experiment*, but rather than the entire parameter-space, it has one specific set of parameters from within the space.

## Server, Workers, & Client

The system architecture looks like this:

```
                            ┌───────────────────┐       run        ┌──────────┐
                            │                   │ ───────────────► │          │
                            │                   │                  │ worker 1 │
                            │                   │ ◄─────────────── │          │
                            │                   │     results      └──────────┘
                            │                   │                              
                            │                   │                              
                            │                   │                              
                            │                   │       run        ┌──────────┐
┌────────┐    experiment    │                   │ ───────────────► │          │
│        │ ───────────────► │      server       │                  │ worker 2 │
│ client │                  │                   │ ◄─────────────── │          │
│        │ ◄─────────────── │                   │     results      └──────────┘
└────────┘     results      │                   │                              
                            │                   │                       .      
                            │                   │                       .      
                            │                   │                       .      
                            │                   │                       .      
                            │                   │                       .      
                            │                   │                              
                            │                   │       run        ┌──────────┐
                            │                   │ ───────────────► │          │
                            │                   │                  │ worker n │
                            │                   │ ◄─────────────── │          │
                            └───────────────────┘     results      └──────────┘
```

The *server* exposes a REST-interface which both the client and workers use to submit and retrieve data.
The client can submit experiments and retrieve results (from either an entire experiment, or a single Run).
Workers periodically try to request a waiting Run from the server.
If work is available, the worker will run the Run and, upon completion, submit the Run's results to the server.
Since the worker will essentially execute arbitrary code, one should never submit untrusted code.
Worker sandbox/isolation is out-of-scope of this project and needs to be considered by the operator (e.g. using Docker, VMs).
Encryption/authentication of communication is, at this point, out-of-scope of this project.

## Experiment submission

When submitting an Experiment, the client needs the following files:

- The Experiment's metadata - stored in a TOML file. This file contains:
  - The Experiment's name
  - The parameter set
  - The name of the executable/script that should be run in each instance
  - A list of file/directory names that will contain the results and should be saved/uploaded by the worker
  - The name of an optional setup executable/script, which will be run before the actual program. This is meant to check for/install dependencies and setup environments.
  - An optional list of dependency file/directory names and where they should be put in the worker's filesystem.
- The Experiment executable
- The optional setup executable (name must match the metadata)
- Optional dependency files/directories (names must match metadata)

The client then creates a zip archive containing all these files.
The archive is then uploaded to the server via its REST interface.
The server then creates the Experiment's Runs by computing the cross product of the Experiment's parameter set.

## Worker Run retrieval

When a worker is idle, it will periodically check with the server for work.
If the server has at least one Run that has not been dispatched to a worker, it will dispatch such a run to the requesting worker.
The worker receives the specific Run's metadata (specifically its unique combination of parameters), as well as the parent Experiment's archive.
The worker will unpack the archive, copy all optional dependency files/directories to their specified locations (if any exist), run the setup executable (if it exists), and finally run the main executable, providing it with the concrete set of parameters for this Run.
(If any of these steps fails for any reason, this worker will abort and notify the server of the failure. It will attach any logs/error messages that were produced)
Once the main executable has terminated, the worker will create a zip archive from all the files/directories defined in the Experiment's result-list and upload this archive to the server.
Should the main executable fail - either by abnormal process termination or by exiting with a non-zero exit code - the worker will still collect any available results, as well as any log message and upload them to the server, while specifying that the Run failed.

## Result retrieval

The client can retrieve results for both an entire Experiment, and for specific runs.
When the client requests results, the server will send it the result archive+run logs (if single Run requested), or all result archives+run logs (if Experiment requested).
